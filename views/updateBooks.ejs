<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Update Book</title>
    
</head>
<body>
    <%- include('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>Update Book</h2>
        <form action="/updateBooks/<%= books.bookid %>" method="POST" enctype="multipart/form-data">
            <div class="mb-3">
                <label for="name" class="form-label">Book Name:</label>
                <input type="text" class="form-control" id="name" placeholder="Enter book title" name="name" value="<%= books.name %>" required>
            </div>
            <div>

            <label for="image"> Current Image:</label><br>
                <input type="text" name="currentImage" value="<%= books.image %>" disabled><br><br>
                <img src = "../images/<%= books.image%>" width="20%"><br><br>

                New Image:<br> <input type="file" id="image" name="image" accept="image/*" value=<%=books.image %><br><br>
            </div>
            
            <div class="mb-3">
                <label for="author" class="form-label">Author:</label>
                <input type="text" class="form-control" id="author" placeholder="Enter author" name="author" value="<%= books.author %>" required>
            </div>
            <div class="mb-3">
                <label for="summary" class="form-label">Summary:</label>
                <textarea class="form-control" id="summary" placeholder="Enter summary" name="summary" rows="3"><%= books.summary %></textarea>
            </div>
            <div class="mb-3">
                <label for="rating" class="form-label">Rating (out of 5):</label>
                <select class="form-control" id="rating" name="rating" required>
                    <option value="">Select a rating</option>
                    <% for(let i = 1; i <= 5; i++) { %>
                        <option value="<%= i %>" <%= books.rating == i ? 'selected' : '' %>><%= i %></option>
                    <% } %>
                </select>
            </div>
            <div class="mb-3">
                <label for="review" class="form-label">Review:</label>
                <textarea class="form-control" id="review" placeholder="Enter review" name="review" rows="3"><%= books.review %></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</body>
</html>
